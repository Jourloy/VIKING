function RCL2RoomBuilder(room) {
    // const extensions
    // const roads
    // TODO: 
}

/**
 * @param {Object} room
 */
function Autobuild(room) {
    switch(room.controller.level) {
        switch 2:
            if (!room.memory.build) RCL2RoomBuilder(room);
            break;
    }
}
