// Example.js

const options = {
    name: 'EXAMPLE',
    target: 'example',
    move: {
        heuristicWeight: 1.4
    }
}

const creep = new Creep(options);

creep.run = function() {
    // Creep logic
}