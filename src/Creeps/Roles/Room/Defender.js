/**
 * When defend flag was plased, then spawn start create this flag. When all hostile
 * creep's will be destroyed, then creep delete flag and go to spawn and do recycle
 *
 * @param {Creep} creep
 */
function Defender(creep) {
    if (!creep.memory.room) Error(`Creep [id: ${creep.id}] have not home room in memory`);
    else {

        // TODO: Code a defender

    }
}
